# Site Notes

## Bibliography

The selected papers section uses `--group_by none`, so jekyll-scholar's `--sort_by` has no effect. Entry order in `_bibliography/papers.bib` controls display order directly. Keep entries in reverse chronological order (newest first) when adding new publications.
